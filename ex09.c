#include <stdio.h>

    int main()
    {
        char parola;
        printf("Scrivi una parola a tuo piacimento ed io la riscriverò in CAPS LOCK!\n");
        scanf("%s", &parola);
        
        return 0;
    }